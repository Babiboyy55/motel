<UserControl x:Class="baitaplon.Views.Admin.UserControls.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#F5F6FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Dòng 1: Tiêu đề -->
            <RowDefinition Height="Auto"/>
            <!-- Dòng 2: 4 Thẻ Card -->
            <RowDefinition Height="*"/>
            <!-- Dòng 3: Không gian cho Bảng danh sách sau này -->
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="DASHBOARD TỔNG QUAN" FontSize="26" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="Tháng:" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="14" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding DanhSachThang}" SelectedItem="{Binding ThangLoc}" Width="60" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

                <TextBlock Text="Năm:" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="14" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding DanhSachNam}" SelectedItem="{Binding NamLoc}" Width="80" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

                <Button Content="Lọc Dữ Liệu" Command="{Binding LocDuLieuCommand}" Background="#34495E" Foreground="White" Padding="15,8" BorderThickness="0" Margin="0,0,10,0" FontWeight="Bold" Cursor="Hand"/>
                <Button Content="Xuất Excel" Command="{Binding XuatExcelCommand}" Background="#27AE60" Foreground="White" Padding="15,8" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <!-- 4 Thẻ Thống kê -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Thẻ 1: Phòng trống (Màu Xanh lam) -->
            <Border Grid.Column="0" Background="#3498DB" CornerRadius="10" Padding="20" Margin="5" 
                    BorderBrush="#2980B9" BorderThickness="0,0,0,4">
                <StackPanel>
                    <TextBlock Text="PHÒNG TRỐNG" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="{Binding SoPhongTrong}" Foreground="White" FontSize="36" FontWeight="Bold"/>
                        <TextBlock Text="/" Foreground="#E0E0E0" FontSize="20" Margin="5,15,5,0"/>
                        <TextBlock Text="{Binding TongSoPhong}" Foreground="#E0E0E0" FontSize="20" Margin="0,15,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Thẻ 2: Tỷ lệ lấp đầy (Màu Xanh lá) -->
            <Border Grid.Column="1" Background="#2ECC71" CornerRadius="10" Padding="20" Margin="5"
                    BorderBrush="#27AE60" BorderThickness="0,0,0,4">
                <StackPanel>
                    <TextBlock Text="TỶ LỆ LẤP ĐẦY" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="{Binding TyLeLapDay}" Foreground="White" FontSize="36" FontWeight="Bold"/>
                        <TextBlock Text="%" Foreground="White" FontSize="24" Margin="5,10,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Thẻ 3: Doanh thu tháng này (Màu Vàng) -->
            <Border Grid.Column="2" Background="#F1C40F" CornerRadius="10" Padding="20" Margin="5"
                    BorderBrush="#F39C12" BorderThickness="0,0,0,4">
                <StackPanel>
                    <TextBlock Text="DOANH THU THÁNG" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding DoanhThuThangNay, StringFormat={}{0:N0} đ}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,10,0,0" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>

            <!-- Thẻ 4: Tổng công nợ (Màu Đỏ) -->
            <Border Grid.Column="3" Background="#E74C3C" CornerRadius="10" Padding="20" Margin="5"
                    BorderBrush="#C0392B" BorderThickness="0,0,0,4">
                <StackPanel>
                    <TextBlock Text="TỔNG CÔNG NỢ" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TongCongNo, StringFormat={}{0:N0} đ}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,10,0,0" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Khung chứa Danh sách nợ & Lịch sử thanh toán -->
        <Grid Grid.Row="2" Margin="5,20,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Column="0" Grid.Row="0" Header="DANH SÁCH CÔNG NỢ" Margin="0,0,10,10" FontWeight="Bold" Foreground="#C0392B" BorderBrush="#E74C3C">
                <DataGrid ItemsSource="{Binding DanhSachCongNo}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" RowBackground="#FFF9F9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tháng" Binding="{Binding ThangNam}" Width="80"/>
                        <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TongTien, StringFormat={}{0:N0}}" Width="*"/>
                        <DataGridTextColumn Header="Còn nợ" Binding="{Binding CongNo, StringFormat={}{0:N0}}" Width="*" Foreground="Red"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="1" Grid.Row="0" Header="10 GIAO DỊCH GẦN NHẤT" Margin="10,0,0,10" FontWeight="Bold" Foreground="#27AE60" BorderBrush="#2ECC71">
                <DataGrid ItemsSource="{Binding LichSuThanhToan}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" RowBackground="#F9FFF9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ngày thu" Binding="{Binding NgayThanhToan, StringFormat=dd/MM/yyyy}" Width="*"/>
                        <DataGridTextColumn Header="Tháng" Binding="{Binding ThangNam}" Width="80"/>
                        <DataGridTextColumn Header="Số tiền thu" Binding="{Binding SoTienDaTra, StringFormat={}{0:N0}}" Width="*" Foreground="Green"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Header="THỐNG KÊ PHÒNG BAN / KHU VỰC" Margin="0,10,0,0" FontWeight="Bold" Foreground="#2980B9" BorderBrush="#3498DB">
                <DataGrid AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên Phòng ban / Khu vực" Width="*"/>
                        <DataGridTextColumn Header="Tổng số phòng" Width="150"/>
                        <DataGridTextColumn Header="Phòng đang thuê" Width="150"/>
                        <DataGridTextColumn Header="Tỷ lệ lấp đầy" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>