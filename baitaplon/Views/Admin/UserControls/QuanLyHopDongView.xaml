<UserControl x:Class="baitaplon.Views.Admin.UserControls.QuanLyHopDongView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="#F5F6FA"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

    <Grid Margin="20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,10,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="QUẢN LÝ HỢP ĐỒNG" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                    <Button Content="+ Tạo Hợp Đồng Mới" Click="BtnMoFormTao_Click" Background="#27AE60" Foreground="White" Padding="15,8" BorderThickness="0" FontWeight="Bold" Cursor="Hand" Margin="0,0,10,0"/>
                    <Button Content="Gia Hạn" Command="{Binding GiaHanCommand}" Background="#3498DB" Foreground="White" Padding="15,8" BorderThickness="0" FontWeight="Bold" Cursor="Hand" Margin="0,0,10,0"/>
                    <Button Content="Thanh Lý" Command="{Binding ThanhLyCommand}" Background="#7F8C8D" Foreground="White" Padding="15,8" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                </StackPanel>

                <DataGrid Grid.Row="2" ItemsSource="{Binding DanhSachHopDong}" SelectedItem="{Binding SelectedHopDong}" AutoGenerateColumns="False" IsReadOnly="True" Background="White" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã HĐ" Binding="{Binding HopDongID}" Width="80"/>
                        <DataGridTextColumn Header="Ngày Bắt Đầu" Binding="{Binding NgayBatDau, StringFormat=dd/MM/yyyy}" Width="*"/>
                        <DataGridTextColumn Header="Ngày Kết Thúc" Binding="{Binding NgayKetThuc, StringFormat=dd/MM/yyyy}" Width="*"/>
                        <DataGridTextColumn Header="Giá Cố Định" Binding="{Binding GiaCoDinh, StringFormat={}{0:N0}}" Width="*"/>
                        <DataGridTextColumn Header="Trạng Thái" Binding="{Binding TrangThai}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="20" Margin="10,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="🔔 LỊCH NHẮC VIỆC" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding DanhSachNhacViec}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FDFEFE" BorderBrush="{Binding MauSac}" BorderThickness="4,0,0,0" CornerRadius="3" Padding="10" Margin="0,0,0,10" >
                                    <Border.Effect>
                                        <DropShadowEffect Color="Gray" Opacity="0.1" ShadowDepth="2" BlurRadius="5"/>
                                    </Border.Effect>

                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                            <TextBlock Text="{Binding Icon}" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding TieuDe}" FontWeight="Bold" Foreground="{Binding MauSac}"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding NoiDung}" TextWrapping="Wrap" Foreground="#555555" FontSize="12"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>